import { Typography } from '@mui/material';
import React from 'react';

const Notiche = () => {
    return (
        <Typography>
            notiche
        </Typography>
    );
};

export default Notiche;